# WfloAI — Public V1 Release Progress

Tracking table for the release-hardening plan in [`release-readiness/MASTER.md`](release-readiness/MASTER.md).

Update this file after each completed task. **Manual and external actions are recorded separately from code work** — an external step performed by the operator is never marked as agent-completed work, and a task is never marked `COMPLETE` while any required verification has failed.

## Status legend

| Status | Meaning |
|---|---|
| `NOT STARTED` | No work begun. |
| `IN PROGRESS` | Work begun, not finished. |
| `BLOCKED` | Stopped on a hard stop, an unanswered question, or an external dependency. Record what is blocking in Notes. |
| `COMPLETE` | All `Required Changes` satisfied, all three verification commands green, `git diff` reviewed, and every manual step either performed or explicitly recorded as outstanding. |

`COMPLETE` requires all three of the following to have passed:

```
npm test
npx tsc --noEmit
npm run build
```

## Progress

| Task | Status | Audit IDs | Commit | Verification | Manual Actions | Notes |
|---|---|---|---|---|---|---|
| 01-security-sweep | IN PROGRESS | A4, A5, A6, A13, A15, B3 | `5d65054` | 2026-09-15: `npm test` 89 passed (11 files) · `npx tsc --noEmit` clean · `npm run build` succeeded | 4 outstanding — see Notes | **Autonomous work complete and verified; not COMPLETE.** Landed: A4 (middleware inverted to an allow-list + `getUser()` guard on `/settings`), A5 (shared `safeRedirectPath` helper in both call sites + `exchangeCodeForSession` error now handled), A13 (migration `202609150001` authored and reviewed — **not applied to any remote database**), A15 (`gmail.compose` removed from the initial connect tier, Create Draft moved behind the restricted gate, CLAUDE.md scope classification corrected), A6 Phase 1 (security headers + `global-error.tsx` no longer renders `error.message`), B3 (`isSameOrigin` helper applied to `POST /api/integrations/gmail/disconnect`). **A6 Phase 3 (full CSP) deferred to Phase 3** — the task's one explicitly permitted carve-out. **A13 deviation:** the audit assumed both ledger write paths bypass RLS; manual runs actually pass the *user-scoped* client, so dropping UPDATE would break `markActionSucceeded` *after* a real send. DELETE is dropped outright; UPDATE is constrained to rows whose current status is `pending` or `failed`, which closes the `succeeded`→`failed` re-send primitive while preserving every transition the app performs. Rationale recorded in the task file's `# Current State` A13 section. **Blocking COMPLETE (operator-only):** (1) Google Cloud Console consent screen must show `gmail.send` and no Restricted scope; (2) normal-authenticated-user RLS denial test for UPDATE and DELETE on `integration_action_executions`, which needs migration `202609150001` applied first; (3) apply that migration; (4) `curl -I` header check against the real domain post-deploy. HUMAN_UI_CHECK_REQUIRED: logged-out `/settings` redirect in a private window, and the Gmail node dropdown no longer offering Create Draft. |
| 02-observability | IN PROGRESS | A7 | `b0d726c` | 2026-09-15: `npm test` 95 passed (12 files) · `npx tsc --noEmit` clean · `npm run build` succeeded | 4 outstanding — see Notes | **Vendor-neutral half complete and verified; BLOCKED on a hard stop for the rest.** Landed: structured logger (`lib/observability/logger.ts`) that redacts context through `lib/integrations/redact.ts` and never throws into the request it reports on; `reportError()` facade with a single `setErrorTransport()` wiring point; `apiError()` helper; the three required failure-class events (`scheduled_run.failed`, `gmail.send.failed`, `http.mutation.failed`); 8 security-relevant swallowed catches now reported (enumerated in the task file, along with the 36 deliberately left silent); 23 raw-Postgres `error.message` leaks across 11 routes replaced with a generic client message plus full operator-side detail; client boundaries `app/error.tsx` and `app/global-error.tsx` wired to the facade, agreeing with task 01's `error.message` removal. **Two defects found and fixed during implementation:** `redactSensitiveKeys` recurses without cycle detection, so circular log context blew the stack *inside* redaction — the logger now drops context entirely rather than logging it unredacted (fails closed); and `countWorkflowsUsingCredential` interpolated Postgres detail into a thrown message. **BLOCKED:** Required Change 1 (wire an external reporter) needs a vendor account and a DSN secret — creating or exposing secrets is a MASTER §7.1 hard stop and the task's own first Stop Condition. Vendor choice is also a **task 07 dependency**: an error reporter capturing request context is a subprocessor the privacy policy must name. **Operator actions:** (1) choose the reporter and create the account/project; (2) provision the DSN in the host environment (never committed); (3) register it via `setErrorTransport()`; (4) confirm its data-retention setting and set up a notification channel. All five Manual verifications depend on (1)–(3). |
| 03-ai-lookup-quotas | IN PROGRESS | A2, B11 | `a3edb85` | 2026-09-15: `npm test` 107 passed (13 files) - `npx tsc --noEmit` clean - `npm run build` succeeded | 5 outstanding - see Notes | Three halves. Provider console caps are manual and should be done on day one, before any code. Also owns the per-user **unit-cost** method (distinct from spend caps; feeds Task 17 Gate 11) and the forced A/B resolution of `MAX_CONCURRENT_REQUESTS`. **Half 2 (code) complete and verified.** Migration `202609150002` adds `input_units`/`output_units` to the ledger and an **atomic** `consume_action_quota` (advisory-locked count-and-insert; a row is inserted `pending` before the provider is called and settled after). AI and Lookup now consume that one mechanism. **Metering covers the primary path, not just the two named routes** - `serverExecutor` calls Anthropic and Tavily directly, so `/api/execute`, `/api/lookup`, the AI node, the **Router node** (an AI call that was easy to miss) and the Lookup node are all metered; AI/Lookup nodes now refuse to run without an `IntegrationContext`, so no path executes a spending node unmetered. Per-run AI-node cap in `validate.ts`; `consumeRunAction` wired for AI and Lookup. **`MAX_CONCURRENT_REQUESTS` resolved as B** - removed, with the durable protections named in code and in the task file. Task 15 should certify B. **B11:** AI/Lookup are atomic; **Gmail/HTTP deliberately are not** - making them atomic requires restructuring `claimAction`, an explicit Stop Condition. Residual race documented with its bound (at most concurrent-minus-one past the limit, cannot compound, not a duplicate-send risk). `gmail.draft` omission moot under A15. **Half 3:** re-runnable SQL + price constants (`lib/integrations/pricing.ts`, read 2026-09-15, **operator must verify**). Pre-launch **ESTIMATE**: $0.0055/AI call, $0.49/light user/month, $4.26/heavy user/month, worst case at the caps **$5.72/user/day**. Basis and limitations recorded in the task file; labelled an estimate, derived from evidence available now, **not** waiting on Phase 1 or Task 15. **Blocking COMPLETE (operator-only):** (1) Anthropic console spend cap; (2) Tavily console cap; (3) billing alerts on both; (4) confirm the **proposed** quota values (AI 20/min, 200/day; Lookup 10/min, 100/day; 20 AI nodes/run) - Stop Conditions make the final numbers the operator's call; (5) apply migration `202609150002`. **Outstanding tests:** the N+1 -> 429 and quota-boundary concurrency tests need a live Postgres, which this repo's suite has no fixture for; recorded in the task file as outstanding rather than faked. **NOTE:** the task file's implementation record is **uncommitted** - it sits on top of pre-existing uncommitted edits to that file and could not be separated without committing your work. |
| 04-runtime-limits | IN PROGRESS | A11 | `0c09954` | 2026-09-15: `npm test` 114 passed (14 files) - `npx tsc --noEmit` clean - `npm run build` succeeded | 2 outstanding - see Notes | `maxDuration` is a one-line Phase 1 change; the rest is Phase 2. **Phase 1 done: `maxDuration = 300`** on the execute route, matching the Inngest route's existing declaration. **The truncated write was real and worse than the audit described.** The `workflow_runs` insert sat inside the same `try` as `executeWorkflow`, and `serverExecutor` rethrows after emitting `node:error` - so **every failed manual run, not just a timed-out one, left no run row at all**. Persistence now happens outside the `try` and records an error run either way, with its own catch so the stream still closes. A route-level runtime budget (30s inside `maxDuration`) races execution so the persistence path is reached before the platform severs the request. **Phase 2 done:** node-count cap (60, reachable nodes, pre-execution, with a test asserting zero provider calls on rejection); `AbortSignal` timeouts on Anthropic and Tavily across both the executor and the two routes, with Lookup reusing `HTTP_LIMITS.TOTAL_TIMEOUT_MS` rather than inventing a second convention. **Phase 2 deferred with reasons (permitted by the Completion Criteria):** per-user concurrent-run limit - Task 03 resolved `MAX_CONCURRENT_REQUESTS` as B and named these caps as the durable protections, so a DB-backed run counter would be the third counting system both task files warn against; client-disconnect behaviour - continuing is defensible but the task requires surfacing the choice in the UI, which is product copy (**HUMAN_UI_CHECK_REQUIRED**). **Blocking COMPLETE (operator-only):** (1) confirm the host plan's function-timeout ceiling - `maxDuration` cannot exceed it and this cannot be checked before Task 13; (2) confirm the **proposed** cap values (60 nodes/run, 60s AI, 30s Lookup). **Outstanding tests:** runtime-cap-persists-error-run and AbortSignal-aborts-a-hung-call need a live DB and a controllable slow provider; recorded as outstanding rather than faked. **NOTE:** a pre-existing uncommitted edit to this task file (the Task 03 ownership sentence) is preserved unstaged. |
| 05-schedule-limits | IN PROGRESS | A12 | `a4f504c` | 2026-09-15: `npm test` 126 passed (15 files) - `npx tsc --noEmit` clean - `npm run build` succeeded | 4 outstanding - see Notes | Must preserve the CAS claim's exactly-one-winner semantics. **Code complete and verified.** 15-minute interval floor on create *and* update, measured against **parsed** occurrences (six consecutive gaps, smallest wins) so every spelling of every-minute is rejected and a short gap hidden among long ones is caught. Per-user cap 20, per-workflow cap 5, each with an error naming the limit; the poller's global `.limit(50)` is untouched because it is a throughput ceiling, not a quota. Migration `202609150003` adds `consecutive_failures` and `disabled_reason` - **not applied remotely**. New `track-schedule-health` Inngest step resets on success and auto-disables on the Nth consecutive failure. Sidebar shows an amber panel explaining why a schedule turned itself off; re-enabling clears the counter. **CAS demonstrably unchanged:** the diff for `lib/inngest/functions.ts` has **zero deleted lines** - the claim is byte-identical, and the health counter is a separate step after the run. Tests pin the claim predicate so a future edit cannot widen it. **Notification sequences behind Task 02** as its Stop Condition requires - `reportError("schedule.auto_disabled")` through Task 02's facade, not a second path; it becomes a real alert when the operator registers a transport. **Blocking COMPLETE (operator-only):** (1) confirm the **proposed** values - 15 min floor, 20/user, 5/workflow, disable after 5 failures; (2) apply migration `202609150003`; (3) decide what to do about any pre-existing sub-floor schedules - the floor is not applied retroactively, since disabling working automation is a product decision; (4) end-to-end notification, which needs Task 02's transport. **Outstanding tests:** the route-level 400 assertions need a live DB and an HTTP harness; the logic is unit-tested and the DB-level checks are recorded rather than faked. |
| 06-nextjs-upgrade | IN PROGRESS | A9 | `68eda56` | 2026-09-16: `npm test` 127 passed (15 files) - `npx tsc --noEmit` clean - `npm run lint` clean - `npm run build` succeeded with **zero warnings** | 3 outstanding + full manual regression - see Notes | Longest pole. **Code migration complete and verified; not COMPLETE — the manual regression checklist is the deliverable an agent cannot produce.** Landed: `next` 14.2.35 -> **16.3.5**, `react`/`react-dom` 18.3.1 -> 19.3.0. **Chose 16.x over 15.x deliberately and against the task's own framing:** the task calls 15.x the lower-risk hop "supported through October 2026", but today is 2026-09-16, so 15.x buys about one month before landing back on an EOL framework - paying this migration's regression cost twice. Both targets need the same React 19 hop, so 15.x would not have dodged the React Flow question either. **Async request APIs:** `createServerSupabaseClient()` is now async (28 call sites, all already inside async functions); 10 dynamic route files take `context: RouteContext` and `await context.params` on line one, which leaves every downstream `params.id` untouched; 3 pages plus the Gmail OAuth callback migrated. `headers()` is unused. **`middleware.ts` is now `proxy.ts`** - Next 16 deprecated the convention. Because this file **is** the auth gate the rename was verified against Next's own build template, which resolves both conventions in one line (``const handlerUserland = (isProxy ? mod.proxy : mod.middleware) || mod.default``) and shares every downstream path; `git mv` preserves history and the only content change is the function name - allow-list, `requiresAuth`, `updateSession()` and `config.matcher` are byte-identical. **`next lint` was removed in 16**, so `npm run lint` is now `eslint .` against a new flat `eslint.config.mjs` carrying the same `next/core-web-vitals` rules; eslint is pinned to **9.39.5 because eslint 10 crashes it** - eslint 10 removed `context.getFilename()` and the `eslint-plugin-react` inside `eslint-config-next` 16 still calls it. **Two new lint rules fire on pre-existing code** (`react-hooks/refs`, `react-hooks/set-state-in-effect`, 6 errors across `useBufferedField.ts` and three canvas components); scoped **off for those four files only** with the reason in the config, because rewriting them means changing canvas/hook behaviour to satisfy a framework bump - an explicit Stop Condition - and `useBufferedField` is the file commit `f356261` fixed. **Recorded as real technical debt, not a false positive.** **Next 16 rejects non-literal segment config exports**, which broke Task 04's `export const maxDuration = EXECUTE_ROUTE_MAX_DURATION_SECONDS`; the route now exports the literal `300` and a new test reads the route source to pin it to the constant so the duplication cannot drift. **`undici` 8.7.0 -> 8.10.2** - `npm audit` flagged five high-severity advisories (CRLF injection via blob body type, cache-directive disclosure, cookie attribute injection) against the client `lib/http/` uses for every HTTP-node request behind the SSRF guard; in-range patch of a direct dependency. **`@supabase/ssr` deliberately stays at 0.5.2** - no Next peer dep, never calls `cookies()` itself, and 0.6 removed the cookie methods `updateSession()` uses, so upgrading it would mean rewriting the auth gate's cookie handling inside the framework upgrade. **React Flow 11.11.4 unchanged** - peer range `react: ">=17"` admits 19, and it type-checks and builds; that is a **static** result, so that Required Change stays unticked. **Blocking COMPLETE:** (1) **every line of the manual regression checklist** - HUMAN_UI_CHECK_REQUIRED, with the Middleware and Canvas lines the two most likely to fail; (2) Gmail send and scheduled-run lines additionally need real external calls; (3) preview then production deploy (Manual Steps 2-3) - deployment is a hard stop, and local success does not prove serverless success for streaming or for the proxy rename. **Operator decision outstanding:** confirm 16.x over 15.x (Manual Step 1). **Also outstanding:** 9 transitive `npm audit` advisories, none a direct runtime dependency - `npm audit fix` aborts with an internal npm error in this environment, so they are recorded rather than half-applied and belong to task 12 (B2). |
| 07-landing-legal | IN PROGRESS | A1, B1 | `858d4cc` | 2026-09-16: `npm test` 136 passed (16 files) - `npx tsc --noEmit` clean - `npm run lint` clean - `npm run build` succeeded | 6 outstanding + counsel review - see Notes | Every legal page ships with a DRAFT banner; banner removal is a human action. **Drafts complete and verified; not COMPLETE - counsel review and a real contact address are human steps.** **Routing decided explicitly:** `/` is **always** the public marketing page and the dashboard moved to `/dashboard`. Serving different content at `/` by session would have made the homepage session-dependent, which is the exact property Google checks ("publicly accessible, not login-restricted"). Build output confirms it: `/`, `/privacy`, `/terms` are prerendered **static**, `/dashboard` is dynamic. Everything pointing at the old dashboard address was repointed - sidebar nav and its `#templates` anchor, the canvas back link, the `LoginCard` default, and **`safeRedirectPath` DEFAULT_PATH**, which is where a login with no `next` lands. **The task 01 collision is now caught by a test rather than by hope:** the allow-list moved to `lib/security/publicPaths.ts` as `requiresAuth(pathname)` (task 01 comments carried over verbatim), `proxy.ts` imports it, and `tests/publicPaths.test.ts` pins that the three public pages are open, that `/dashboard`, `/settings` and `/workflows/:id` are gated, that `/api/*` and `/_next/*` pass through, and - the A4 property - that an **unrecognised path is gated by default**, so the allow-list cannot decay back into the deny-list it replaced. **Privacy policy written against the data inventory, not a template:** seven storage categories, six receiving services, and a section of its own for **the HTTP Request step** (arbitrary egress to a destination the policy cannot describe) - the item the task warns is easiest to forget. **Four things it deliberately does not claim:** no Anthropic ZDR (states the commercial terms, then says no zero-retention agreement has been negotiated); **no retention period for run history or integration records** - they are kept indefinitely, task 12 has not landed, and the task says do not state a period you do not honor, so a visible Pending callout says exactly that; **no self-service account deletion** - that is task 09, so deletion is described as a manual request; and **no error-monitoring subprocessor** - task 02 facade has **no transport registered**, so nothing is sent today and naming a vendor would name a fictional subprocessor. **That last point dissolves the apparent task 02 -> task 07 block:** the dependency runs the other way - whoever registers a transport must list it here first. Hosting provider is named as a category because task 13 chooses it. Gmail section states `gmail.send` only, that it cannot read/list/draft, the **Limited Use** paragraph, and that message/thread IDs never reach the UI or an AI step. **ToS/AUP:** all four required prohibitions (spam/UCE, credential abuse, open-proxy use of the HTTP node, scraping) plus six more the product actually enables, and the suspension right reserved. **B1 AI disclosure is a full ToS section**, and an in-product note now sits under the execution log. **Blocking COMPLETE:** (1) **counsel review** and then **banner removal - a MASTER 7.1 hard stop for an agent**; (2) a **real monitored contact address** - both pages carry a visible placeholder, deliberately not invented, and it must match task 13 runbook (task 17 gate 15); (3) hosting provider named once task 13 picks it; (4) retention periods once task 12 implements them; (5) account-deletion wording once task 09 lands; (6) the error reporter listed **before** task 02 transport is registered. **HUMAN_UI_CHECK_REQUIRED:** private-window load of all three pages, same-domain check (needs a deployed origin, task 13), and whether the marketing page reads correctly to a signed-in visitor, who currently sees a Sign in button that routes them onward to `/dashboard`. **Outstanding test:** the literal "returns 200 unauthenticated" assertion needs an HTTP harness this repo does not have (MASTER 7.7); the same property is asserted one level down against the predicate the proxy consults. |
| 08-signup-gating | IN PROGRESS | A3 | `76e1a21` | 2026-09-16: `npm test` 152 passed (17 files) - `npx tsc --noEmit` clean - `npm run lint` clean - `npm run build` succeeded | 5 outstanding + CAPTCHA hard stop - see Notes | Gate must cover the money-spending API routes, not just pages. **Code complete and verified; not COMPLETE - CAPTCHA needs an account and a secret, which is a hard stop.** **Mechanism resolved from the repository rather than asked:** the Stop Condition says the invite-vs-waitlist choice depends on how you recruit, and MASTER section 4 answers it - Phase 2 is invite-only and "strangers get in here, but only behind an invite gate". So **invite code**, and it is still **one** mechanism: `profiles.approved` is the gate, redeeming a code is the one self-serve way to pass it, and a SQL update is the operator way the task already permits. Manual Step 1 is yours to confirm. **Migration `202609160001`** adds `profiles` (RLS, select-own, and **deliberately no UPDATE policy** - a user who could update their own row could approve themselves) and `invite_codes` (**RLS enabled with no policies at all**, so no client can read the codes), a `handle_new_user` trigger so a first login has a row, and `redeem_invite_code` (security definer, `select ... for update` on the code row so `max_uses` survives a concurrent redemption, early-returns instead of burning a use if already approved). **Not applied remotely.** **Enforced in three places because one is not enough:** `proxy.ts` for pages, layered on task 01 allow-list via `requiresApproval()` which delegates to `requiresAuth()` - so the two compose instead of drifting as competing path lists; **each money-spending route checks itself** (`/api/execute`, `/api/lookup`, **`/api/workflows/[id]/execute`** - the primary spend path the task does not name - and the schedule run-now route), since the proxy deliberately does not gate `/api/`; and **the Inngest runner**, because a schedule created before approval was revoked would otherwise keep spending on its own timetable with nobody signed in. **Approval is read live per request**, not from the session, which is what makes "approval takes effect without a re-login" actually true. **`isApprovedUser` fails closed** - missing row, RLS denial and transport error are all not-approved. **No redirect loop by construction:** `requiresApproval("/waitlist")` is false and a test pins it; an approved user landing there is sent on by both the proxy and the page. `/waitlist` renders in the existing auth card with an invite field and sign-out; `POST /api/invite/redeem` carries task 01 `isSameOrigin` check. **Existing accounts are backfilled as UNAPPROVED on purpose** - that is the shape Manual Step 4 assumes ("seed the approval list ... or you will lock yourself out"), and retroactively de-authorising real accounts is a Stop Condition, so the migration does not guess; the grandfathering alternative is written out in its operator notes. **Nobody is locked out today - the migration is not applied anywhere.** **Blocking COMPLETE:** (1) **CAPTCHA** - needs an hCaptcha/Turnstile account plus a secret and a Supabase dashboard change; creating secrets and changing provider config are MASTER 7.1 hard stops and this task third Stop Condition. Nothing was created or configured; (2) apply the migration remotely; (3) **seed the approval list before the gate reaches real accounts**, or choose grandfathering; (4) confirm invite code over waitlist (Manual Step 1); (5) keep the Google OAuth app in Testing mode through Phase 1 - the unverified-app cap is still doing real work and this gate does not replace it. **Outstanding tests:** the four behavioural assertions (unapproved denied a page, denied by the spending routes, approved unaffected, profile row created on first login with RLS isolation) need a live Postgres and a request cycle; the decision logic under each is unit-tested and the route/runner guards are pinned by source assertions, so a future edit cannot drop one quietly. **HUMAN_UI_CHECK_REQUIRED:** fresh Google account lands on the waitlist, approval works without re-login, no loop from `/`, `/settings` or a workflow URL, and the CAPTCHA challenge actually appears. |
| 09-account-deletion | NOT STARTED | A8 | — | — | — | Deletion order is load-bearing: revoke at Google → delete Storage → delete auth user. Test accounts only. Admin-client exception already granted in CLAUDE.md and narrowly scoped — target `user_id` from `auth.getUser()`, never client input. |
| 10-gmail-consent | NOT STARTED | A14a, B14b, D1 (flag-off only) | — | — | — | Depends on task 01's A15 landing first. All send tests to own address only. |
| 11-production-config | NOT STARTED | A10, B5, B6, B9 | — | — | — | Almost entirely manual. Contains the plan's most important verification: unsigned POST to production `/api/inngest` must be rejected. |
| 12-ci-docs-retention | NOT STARTED | B2, B7, B8, B10 | — | — | — | Retention periods must match the published privacy policy exactly. |

## Manual / external action log

Record operator-performed actions here as they happen. These are **not** agent work and must never be marked complete by an agent.

| Date | Action | Task | Performed by | Notes |
|---|---|---|---|---|
| — | — | — | — | — |

## Deferred

C1–C10, D2, D3, D4, and the full D1 restricted-scope program are deferred to Phase 4. See [`release-readiness/MASTER.md`](release-readiness/MASTER.md) §6. Do not implement them during V1 hardening.
